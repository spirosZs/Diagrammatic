{
  "Logging": {
    "LogLevel": {
      "Default": "Information",
      "Microsoft.AspNetCore": "Warning"
    }
  },
  "DetailedErrors": true,
  "AllowedHosts": "*",
  "//Api": "InternalBaseUrl is called from inside the circuit (HttpClient + SignalR); PublicBaseUrl is rendered into the page and fetched by the browser (diagram editor iframe). Overridden in Docker via Api__InternalBaseUrl / Api__PublicBaseUrl.",
  "Api": {
    "InternalBaseUrl": "http://localhost:8083",
    "PublicBaseUrl": "http://localhost:8083"
  }
}
