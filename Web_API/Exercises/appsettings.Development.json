{
  "//Logging": "Microsoft.EntityFrameworkCore.Database.Command at Debug logs every SQL statement and its parameters. Under a class of students polling the game state that is thousands of log lines a minute, which measurably slows the API down. Raise it deliberately when debugging a query.",
  "Logging": {
    "LogLevel": {
      "Default": "Information",
      "System": "Warning",
      "Microsoft": "Warning",
      "Microsoft.EntityFrameworkCore.Database.Command": "Warning"
    }
  }
}
